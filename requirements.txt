# requirements.txt
# Charter Community Intelligence Platform
# Python 3.11+

# Anthropic SDK
anthropic>=0.40.0

# Environment variable loading from .env
python-dotenv>=1.0.0

# Config parsing
pyyaml>=6.0.1

# Schema validation
jsonschema>=4.23.0

# Layer 2 — Notion soft-signal backend (runtime client)
notion-client>=2.2.1,<3.0.0

# Template rendering (for S7)
jinja2>=3.1.4

# PDF export via headless Chromium
playwright>=1.40.0

# Data processing (S1 roster parsing)
# pandas>=2.2.0  # uncomment if Excel roster format is needed
# openpyxl>=3.1.2  # for .xlsx rosters

# ZIP Drill v2 — geographic / spatial (required for --mode zip_v2)
geopandas>=0.14.0
shapely>=2.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
flask
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
