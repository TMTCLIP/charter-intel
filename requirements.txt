# requirements.txt
# Charter Community Intelligence Platform
# Python 3.11+

# Anthropic SDK
anthropic>=0.40.0

# Environment variable loading from .env
python-dotenv>=1.0.0

# Config parsing
pyyaml>=6.0.1

# Schema validation
jsonschema>=4.23.0

# Template rendering (for S7)
jinja2>=3.1.4

# PDF export (optional — needed for --output-format pdf)
# weasyprint>=62.0  # uncomment if PDF output is needed

# Data processing (S1 roster parsing)
# pandas>=2.2.0  # uncomment if Excel roster format is needed
# openpyxl>=3.1.2  # for .xlsx rosters

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
