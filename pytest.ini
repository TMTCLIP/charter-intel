[pytest]
# Run from project root so relative paths (config/, templates/, data/) resolve.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Markers
markers =
    unit: Pure-Python unit tests — zero API calls
    integration: End-to-end pipeline tests — requires recorded fixtures
