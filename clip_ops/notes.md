# clip_ops manual notes (optional)

Add free-form operational notes as bullet points below. Only bullet lines
(starting with `-`, `*`, `+`, or `1.`) are ingested as backlog candidates and
scored by the deterministic ranker (severity keywords, frequency across CLIP
files, recency of this file). Prose and headings like this paragraph are
ignored, so it is safe to leave instructions here.

To add a note, write a bullet line such as "Confirm CENSUS_API_KEY rotation
before the next statewide batch run". Until you add real bullets below, this
file contributes nothing to the backlog.
